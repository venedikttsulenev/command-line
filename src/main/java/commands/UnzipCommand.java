package commands;

import utils.Environment;

public class UnzipCommand extends Command {


    private UnzipCommand() {
        super("unzip", "unzip <file>", "decompress zip-archive");
    }

    @Override
    public void execute(String[] args, Environment env) {
        /* TODO */
    }
}
